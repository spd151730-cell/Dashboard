import React, { useEffect, useState } from 'react';
export default function TopBar({ mode, onModeChange }) {
  const [clock, setClock] = useState(new Date().toISOString().slice(11, 23));
  useEffect(() => { const timer = setInterval(() => setClock(new Date().toISOString().slice(11, 23)), 100); return () => clearInterval(timer); }, []);
  return <><header className="bg-white border-b-2 border-slate-300 shadow-sm px-6 py-3 flex items-center justify-between sticky top-0 z-20"><div className="flex items-center gap-4"><div className="flex items-center gap-2"><span className="inline-block w-2.5 h-2.5 rounded-full bg-emerald-500 animate-pulse" /><span className="font-headline-sm text-sm tracking-wide uppercase text-slate-800">MALE UAV Aero Piston Engine Workstation</span></div><div className="h-4 w-px bg-slate-200" /><span className="px-2 py-0.5 rounded bg-blue-50 text-blue-900 border border-blue-200 font-semibold text-xs">CRUISE</span></div><div className="flex items-center gap-3"><div className="text-right hidden sm:block"><div className="text-[10px] text-slate-400 font-mono uppercase">Telemetry Clock (UTC)</div><div className="text-xs font-mono font-bold text-slate-700">{clock}</div></div><div className="flex items-center bg-slate-100 p-1 rounded-md border border-slate-200"><button className={`px-3 py-1 text-xs font-bold rounded ${mode === 'live' ? 'bg-white text-slate-800 shadow-sm' : 'text-slate-500'}`} onClick={() => onModeChange('live')}>Live</button><button className={`px-3 py-1 text-xs font-bold rounded ${mode === 'replay' ? 'bg-white text-slate-800 shadow-sm' : 'text-slate-500'}`} onClick={() => onModeChange('replay')}>Replay</button></div><button className="px-3 py-1.5 bg-red-700 text-white text-xs font-bold rounded">MASTER ABORT</button></div></header></>;
}
